const {test}=require('node:test');const assert=require('node:assert/strict');const qr=require('../public/vendor/qrcode.js');const jsQR=require('jsqr');const {createCanvas}=require('@napi-rs/canvas');
test('real encoded QR decodes to published game URL with a four-module quiet zone',()=>{const url='https://zloojesse.github.io/leyan-moon-rabbit/';const q=qr(0,'M');q.addData(url);q.make();const n=q.getModuleCount(),cell=5,c=createCanvas((n+8)*cell,(n+8)*cell),g=c.getContext('2d');g.fillStyle='#fff';g.fillRect(0,0,c.width,c.height);g.fillStyle='#233d35';for(let r=0;r<n;r++)for(let col=0;col<n;col++)if(q.isDark(r,col))g.fillRect((col+4)*cell,(r+4)*cell,cell,cell);const result=jsQR(new Uint8ClampedArray(g.getImageData(0,0,c.width,c.height).data),c.width,c.height);assert.equal(result.data,url);});
